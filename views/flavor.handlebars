<body>
    <main class="flavor-page">
        <div class="greeting">
            <h2 class="center-align">Choose a flavor to get started</h2>
        </div>
        <div class="container flavor-wheel">

        </div>
<div class="container show-on-click-flavor">
            <ul>
                {{#each flavor}}
                <li>{{name}}</li>
                {{/each flavor}}
            </ul>
        </div>
    </main>
</body>